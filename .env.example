# Rename to .env (do NOT commit your real keys)
# VITE_GEMINI_API_KEY is no longer used in the frontend. Use a server-side
# proxy (see README.gemini-proxy.md).
VITE_GEMINI_API_KEY=

# Supabase Configuration (example placeholders)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

APP_URL=
